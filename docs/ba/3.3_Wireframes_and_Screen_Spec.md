# 3.3 — Wireframes & Screen Spec (POINTER FILE)

| Hạng mục | Thông tin |
|---|---|
| Tài liệu | 3.3 Wireframes and Screen Spec |
| Folder | 03_Solution_Requirement |
| Phiên bản | 0.1-pointer (2026-05-20) |
| Trạng thái | 🔗 POINTER — file native BA chưa viết; consolidator các artefact code-repo |
| Liên quan | 3.1 SRS · 3.2 NFRS · 3.4 API Contract · PROJECT_STATUS_2026-05-20.md |

---

## 1. Tại sao là pointer file

Code repo `D:\Kaori System\` đã có một số artefact spec-quality bao quát UI/wireframe:
- `docs/specs/UI_SCREENS_INVENTORY.md` — 70 screens × 6 portal (P1/P2/P3/P4/Shared/Billing)
- `docs/specs/MESSAGE_DEFINITIONS.md` — error codes SYS-ERR* + USR-ERR* + wiring RFC 7807
- `docs/specs/VALIDATION_RULES.md` — per-field input constraints

Nếu copy nội dung sang đây thì sẽ drift ngay khi code repo cập nhật (file UI_SCREENS_INVENTORY thường update theo sprint). Pointer giữ 1 source of truth.

## 2. Source of truth canonical

| Loại | File code-repo | Phạm vi |
|---|---|---|
| Inventory màn hình | `D:\Kaori System\docs\specs\UI_SCREENS_INVENTORY.md` | 70 screens, route, role, mục đích |
| Thông điệp lỗi | `D:\Kaori System\docs\specs\MESSAGE_DEFINITIONS.md` | SYS-ERR* / USR-ERR* + wiring RFC 7807 |
| Validation rule per field | `D:\Kaori System\docs\specs\VALIDATION_RULES.md` | format / range / required / depend-on |
| FE design template (anh provide) | `D:\Kaori Document\frontend template\` | Per-portal design system |

## 3. Phần còn thiếu (gap để native 3.3 đầy đủ)

- **Per-screen mockup PNG/Figma** cho 70 screen — chưa có; phụ thuộc anh restructure FE template trước.
- **User flow journey map A–F** chính thức ở dạng BPMN/UML — code repo có description trong CLAUDE.md / strategic/* nhưng chưa diagram.
- **Workflow Builder canvas mockup** (GAP-02 closeout) — BE đã ship 45 node executor + 25 template, FE drag-drop polish đợi restructure.
- **Org Hierarchy canvas mockup** (GAP-01 closeout) — BE corporate_tree + cross-link đã có, FE canvas đợi.
- **Document viewer + bbox highlight mockup** (GAP-03 closeout) — BE bbox Pattern 5 foundation đã có, FE highlight UI đợi.

## 4. Quy ước cập nhật

1. Khi code repo `docs/specs/UI_SCREENS_INVENTORY.md` update → trang này KHÔNG cần edit (vì là pointer).
2. Khi có mockup PNG/Figma thật → ghi pointer vào §2 + bump version.
3. Khi BA round consolidator → di chuyển nội dung từ code repo `docs/specs/` sang đây với CR explicit.

---

*— Hết 3.3 (pointer) —*
