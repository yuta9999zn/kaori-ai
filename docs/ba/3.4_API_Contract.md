# 3.4 — API Contract (POINTER FILE)

| Hạng mục | Thông tin |
|---|---|
| Tài liệu | 3.4 API Contract |
| Folder | 03_Solution_Requirement |
| Phiên bản | **0.2-pointer** (2026-05-21 EOD3 — bump 169 → 187 endpoint sau Phase 2.5/2.6/2.7/2.8 ship) |
| Trạng thái | 🔗 POINTER — file native BA chưa viết; consolidator các artefact code-repo |
| Liên quan | 3.1 SRS · 3.5 Data Dictionary · 3.6 UAT · 3.7 SA Pointer · 2.2 FRD §2 · CR-0014 (Tier 3 #7 close) |

**Thay đổi vs v0.1:** (1) Bump endpoint count **169 → 187** reflect Phase 2.5/2.6/2.7/2.8 ship (+~30 NEW). (2) Cập nhật Process Mining connector 4 → **10 nguồn** đã ship. (3) Đồng bộ với SA HTML v1.2 §10 source-of-truth dòng "API_CATALOG_V4.md — ~187 endpoints".

---

## 1. Tại sao là pointer file

API là artefact thay đổi theo từng commit. Code repo đã có:
- **`D:\Kaori System\docs\API_CATALOG_V4.md`** — **187 endpoint** chính thức (169 baseline Phase 1+1.5+2 + ~30 NEW Phase 2.5/2.6/2.7/2.8). Cập nhật theo sprint, đối chiếu BACKLOG_V4 + GAPS_V4.
- **`D:\Kaori System\docs\api-specs\orchestrator.openapi.json`** — snapshot OpenAPI 3.1 ai-orchestrator (cần regen tại HEAD `28cc2e4` để khớp 187).
- **`D:\Kaori System\docs\api-specs\pipeline.openapi.json`** — snapshot OpenAPI 3.1 data-pipeline (cần regen).

Copy snapshot vào BA folder sẽ stale ngay khi endpoint mới ship. Pointer giữ chain duy nhất.

## 2. Source of truth canonical

| Loại | File code-repo | Phạm vi |
|---|---|---|
| Catalog endpoint | `D:\Kaori System\docs\API_CATALOG_V4.md` | **187 endpoint** + 42+ dependency cross-service |
| OpenAPI orchestrator | `D:\Kaori System\docs\api-specs\orchestrator.openapi.json` | Generate qua `python scripts/dump_openapi.py` |
| OpenAPI pipeline | `D:\Kaori System\docs\api-specs\pipeline.openapi.json` | Generate qua `python scripts/dump_openapi.py` |
| Convention (envelope / pagination / idempotency / tracing) | `CLAUDE.md` §6 (code repo) | API Design Conventions |
| Auth header forwarding | `CLAUDE.md` §6 + K-7 + K-12 (code repo) | JWT claims → X-* headers |
| Error format | `CLAUDE.md` §6 K-14 + `docs/specs/MESSAGE_DEFINITIONS.md` | RFC 7807 Problem Details |
| MCP tool registry | `D:\Kaori System\docs\specs\CHAT_TOOL_REGISTRY_V4.md` | Tool catalog cho LLM tool-calling K-15/K-16 |

## 3. Endpoint group nhanh (rev 2026-05-21 EOD3)

> Chi tiết per-endpoint phải đọc `docs/API_CATALOG_V4.md` để khỏi drift. Đây chỉ là index nhanh:

| Nhóm | Service | Số endpoint |
|---|---|---|
| Auth (login + JWT + MFA + SSO + field-key rotate + re-encrypt) | auth-service Java + ai-orch SSO router | ~17 |
| Workflow (CRUD + step + folder + step_documents + run + approval + form + import/export + collab + parallel A/B) | ai-orchestrator | ~33 |
| Pipeline (upload + clean + Bronze→Silver→Gold + quality scorecard + OCR + DocSage) | data-pipeline | ~28 |
| RAG + memory + ontology + lineage walk | ai-orchestrator | ~13 |
| **Process Mining + 10 connectors** ⭐ (Postgres CDC/Excel/Zalo/Gmail/**Outlook/Calendar/Slack/Teams/SharePoint/webhook**) | data-pipeline | ~18 |
| Adoption + NOV + economics + interventions | ai-orchestrator | ~14 |
| Corporate tree + workflow cross-link + organization | ai-orchestrator | ~13 |
| Governance Phase 2.7: lineage walk + DLQ console + ai-decision-audit + quota usage + policy engine | ai-orchestrator | ~12 |
| Platform admin (workspace + key + plan + billing monitor + pilot conversion + claims grant) | api-gateway + ai-orch | ~17 |
| **CS Vertical Phase 2.8** ⭐ NEW (tickets + refunds + nps + churn-save) | ai-orchestrator | ~17 |
| LLM Gateway: /v1/infer + /v1/embed + /v1/ocr | llm-gateway | 3 |
| Industry Bootstrap 3-tier (mig 101/102) | ai-orchestrator | ~2 |
| **Tổng** | — | **~187** (đối chiếu API_CATALOG_V4) |

## 4. Phần còn thiếu (gap để native 3.4 đầy đủ — REVIEW_NOTES §2)

- ⏳ **SLA per endpoint** (P50 / P99 latency) — chưa documented per endpoint; chỉ có aggregate trong 3.2 NFRS §2.
- ⏳ **Rate limit per endpoint** — chỉ có aggregate trong K-13 (Idempotency) và Phase 2.7 `tenant_quotas`; chưa per-endpoint table.
- ⏳ **Webhook contract outbound** (FR-NOT-* notification + NOV digest + adoption alert) — chưa documented chính thức.
- ⏳ **API versioning policy chính thức** (`/api/v1/` → `/api/v2/{portal}/` với deprecation window) — CLAUDE.md §6 nói Phase 2+ target, chưa chi tiết.

## 5. Quy ước cập nhật

1. Khi endpoint mới ship code → bắt buộc:
   - Update `docs/API_CATALOG_V4.md` (bump tổng count).
   - Regenerate `docs/api-specs/*.openapi.json` qua `dump_openapi.py`.
   - Regenerate FE TypeScript types (`pipeline.d.ts`, `orchestrator.d.ts`).
   - Add UAT cho endpoint trong `docs/uat/`.
2. Pointer file này KHÔNG cần edit cho mỗi endpoint mới.
3. Bump §3 table count + ngày khi đạt milestone (P2.X / P3 ship lớn).

---

*— Hết 3.4 API Contract (pointer) v0.2 —*
