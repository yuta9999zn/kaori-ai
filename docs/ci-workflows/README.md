# CI Workflows (reference)

These GitHub Actions workflows run on Kaori's private engineering monorepo
(architecture guards, schema-drift gate, OpenAPI/type drift, secret scanning,
per-service test suites). They are included here as reference to show the
engineering pipeline behind this submission.
