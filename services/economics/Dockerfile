FROM python:3.11-slim
WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . /app/economics/

ENV PYTHONPATH=/app

EXPOSE 8098

CMD ["uvicorn", "economics.main:app", "--host", "0.0.0.0", "--port", "8098", "--workers", "1"]
